%h2
  List of Pastes
%table
  - @list.each do |paste|
    %tr
      %td
        #{paste.id}
      %td
        %a{:href => "/#{paste.id}"} #{Sanitize.clean(paste.summary)}
      %td
        #{paste.timestamp}

